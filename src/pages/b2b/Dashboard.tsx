import { ScreenStub } from "@/components/layout/ScreenStub";

export default function B2BDashboard() {
  return (
    <ScreenStub
      eyebrow="B2B / Overwatch"
      title="Manager dashboard"
      description="Team status board, active-alerts map, incident response workflow, and audit log export."
      specRef="CLAUDE.md > B2B Features — tables: organizations, user_locations, incidents"
    />
  );
}

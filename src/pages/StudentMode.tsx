import { ScreenStub } from "@/components/layout/ScreenStub";

export default function StudentMode() {
  return (
    <ScreenStub
      eyebrow="Student Mode"
      title="Bus tracking & walk-home timer"
      description="Live bus ETA, a walk-home countdown, and one-tap hangout check-ins for younger users."
      specRef="CLAUDE.md > Consumer Features > Student Mode"
    />
  );
}

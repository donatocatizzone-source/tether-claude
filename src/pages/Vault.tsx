import { ScreenStub } from "@/components/layout/ScreenStub";

// Port target: reference/tether-app-demo.html #screen-vault (~line 881).
export default function Vault() {
  return (
    <ScreenStub
      eyebrow="Marketplace Mode"
      title="Evidence vault"
      description="Grid of encrypted evidence thumbnails (photos + logs) captured from Marketplace Mode."
      specRef="reference/tether-app-demo.html #screen-vault"
    />
  );
}

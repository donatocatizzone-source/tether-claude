import { ScreenStub } from "@/components/layout/ScreenStub";

export default function Settings() {
  return (
    <ScreenStub
      eyebrow="Settings"
      title="Account & safety word"
      description="Safe word, duress PIN, premium paywall, and notification preferences."
      specRef="reference/tether-app-demo.html #screen-settings (~line 920)"
    />
  );
}
